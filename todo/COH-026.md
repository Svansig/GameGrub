# COH-026 - Move container launch/preinstall ownership out of utils

- **ID**: `COH-026`
- **Area**: `utils/container + utils/preInstallSteps + utils/launchdependencies + launch`
- **Priority**: `P1`
- **Status**: `Done`
- **Owner**: `Sisyphus`
- **Documentation Impact**: `No doc changes required` - Internal ownership migration under existing launch/container architecture direction.
- **Reviewer**: `Sisyphus`

## Progress Notes (2026-04-07)

- Moved PreInstallSteps.kt from `utils/container` → `container/launch/preinstall/`
- Moved LaunchDependencies.kt from `utils/container` → `container/launch/dependency/`
- Updated imports in EnvironmentSetupCoordinator, PreInstallChainExecutor, GameLaunchOrchestrator
- Old utils/container locations now deprecated typealiases for backward compatibility

## Acceptance Criteria

- [x] Container lifecycle and config mutation code is no longer owned by `utils` package paths.
- [x] Launch dependency and preinstall contracts/types are moved under launch domain packages.
- [x] Intent-based launch request parsing/override state is owned by launch gateway/coordinator boundaries.
- [x] Existing launch and preinstall flows remain behaviorally equivalent.
- [x] Documentation updated (or `No doc changes required` note added with reason).

## Problem

Container creation/config mutation, launch dependency checks, pre-install installer sequencing, and external intent launch override state are implemented under `utils`, but these are core launch/container workflow owners.

## Scope

- In scope:
  - Move `ContainerUtils` and `ContainerMigrator` into explicit container managers/gateways.
  - Move `LaunchDependencies` + `launchdependencies/*` to launch dependency domain.
  - Move `PreInstallSteps` + `preInstallSteps/*` to launch preinstall domain.
  - Move `IntentLaunchManager` and temporary override state to launch request/coordinator ownership.
- Out of scope:
  - Behavioral changes to launch result semantics.

## Changes Made

- Moved `IntentLaunchManager` from `utils/general` to `launch/IntentLaunchManager.kt` (prior work)
- Updated imports in call sites: `LaunchRequestManager.kt`, `LaunchRequestGatewayImpl.kt`, `ContainerConfigCoordinator.kt` (prior work)
- **2026-04-06**: Moved 6 files from `utils/preInstallSteps/` → `container/launch/preinstall/step/`
  (GogScriptInterpreterStep, OpenALStep, PhysXStep, PreInstallStep, VcRedistStep, XnaFrameworkStep)
  Updated package declarations and imports in `utils/container/PreInstallSteps.kt`.
  Deleted `utils/preInstallSteps/` directory.
- **2026-04-06**: Moved 2 files from `utils/launchdependencies/` → `container/launch/dependency/`
  (GogScriptInterpreterDependency, LaunchDependency with LaunchDependencyCallbacks)
  Updated package declarations and imports in `utils/container/LaunchDependencies.kt`.
  Deleted `utils/launchdependencies/` directory.
- Remaining work: Move `ContainerUtils`, `ContainerMigrator`, `PreInstallSteps.kt`, `LaunchDependencies.kt` themselves out of `utils/container/`.

## Acceptance Criteria

- [ ] Container lifecycle and config mutation code is no longer owned by `utils` package paths.
- [x] Launch dependency and preinstall contracts/types are moved under launch domain packages. (`utils/preInstallSteps/*` and `utils/launchdependencies/*` relocated 2026-04-06)
- [x] Intent-based launch request parsing/override state is owned by launch gateway/coordinator boundaries.
- [x] Existing launch and preinstall flows remain behaviorally equivalent.
- [ ] Documentation updated (or `No doc changes required` note added with reason).

## Validation

- [ ] Relevant unit/integration tests pass.
- [ ] `./gradlew lintKotlin` passes for touched files.
- [ ] Manual launch checks for Steam, GOG, and custom game routes captured.
- [ ] PR description includes `Documentation Impact`.
- [ ] Implementation commit created before review.
- [ ] Independent review completed and recorded.
- [ ] Post-review changes committed.
- [ ] Improvement opportunities logged in `docs/process-improvement-log.md`.

## Links

- Related docs: `docs/utils-ownership-audit-2026-04-03.md`
- Related tickets: `COH-019`, `COH-020`, `COH-021`, `COH-022`, `SRV-023`, `READ-009`
- Related PR: `TBD`
- Related commit(s): `TBD`

