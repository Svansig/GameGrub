# COH-033 - Replace `GameGrubApp` companion runtime globals with lifecycle-owned state holders

- **ID**: `COH-033`
- **Area**: `app runtime state + ui/xserver interop`
- **Priority**: `P1`
- **Status**: `In Progress`
- **Owner**: `Sisyphus`
- **Documentation Impact**: `Expected global-state migration docs update in docs/process-improvement-log.md and docs/architecture notes.`
- **Reviewer**: `TBD`

## Audit Results

### Current State (84 references across codebase)

GameGrubApp companion object holds these globals:
- `xEnvironment: XEnvironment?` - 26 references
- `xServerView: XServerView?` - 18 references  
- `inputControlsView: InputControlsView?` - 28 references
- `touchpadView: TouchpadView?` - 30 references
- `inputControlsManager: InputControlsManager?` - 4 references
- `achievementWatcher: AchievementWatcher?` - 2 references

### Files with Most References

| File | Reference Count |
|------|-----------------|
| XServerScreen.kt | ~50 |
| XServerInputControlsOverlayHelper.kt | ~15 |
| XServerExitCoordinator.kt | ~5 |
| XServerOverlayActionCoordinator.kt | ~6 |
| MainActivity.kt | ~4 |
| GameGrubMain.kt | ~3 |

### Migration Approach

**Phase 1: Create delegate wrapper (UI state holder)**
1. Create `ui/runtime/XServerRuntimeState.kt` class
2. Add deprecated forwarders in GameGrubApp companion
3. Migrate XServerScreen.kt first (largest consumer)

**Phase 2: Migrate other consumers**
4. XServerInputControlsOverlayHelper.kt
5. XServerExitCoordinator.kt
6. XServerOverlayActionCoordinator.kt

**Phase 3: Clean up**
7. Remove deprecated forwarders after all consumers migrated
8. Update documentation

## Acceptance Criteria

- [ ] Runtime state moved to dedicated holder class
- [ ] All 84 references migrated to new API
- [ ] Memory leak risks reduced (proper lifecycle ownership)
- [ ] Thread-safety improved (atomic access patterns)
- [ ] Documentation updated

## Problem

`GameGrubApp` companion object retains mutable global references to runtime objects and UI views (`xServerView`, `inputControlsView`, `touchpadView`, listener refs), creating memory leak and thread-safety risk.

## Scope

- In scope:
  - Inventory and classify companion globals by ownership and lifecycle.
  - Replace long-lived view/listener references with lifecycle-owned holders and explicit cleanup points.
  - Introduce thread-safe access patterns for remaining shared runtime session state.
- Out of scope:
  - Redesigning Winlator runtime behavior.

## Dependencies and Decomposition

- Parent ticket: `todo/COH-032.md`
- Child tickets: `N/A`
- Related follow-ups: `todo/DOC-015.md`, `todo/TEST-004.md`
- Blocker (if `Blocked`): `N/A`

## Acceptance Criteria

- [ ] `GameGrubApp` companion no longer stores direct `View` or listener references that can outlive their owners.
- [ ] Remaining shared state has explicit owner, lifecycle reset path, and thread-safety strategy.
- [ ] Leak-prone TODO comments in this area are resolved or replaced by tracked owner/action notes.
- [ ] Documentation updated (or `No doc changes required` note added with reason).

## Validation

- [ ] `./gradlew testDebugUnitTest --tests "*Launch*"`
- [ ] `./gradlew lintKotlin`
- [ ] Manual start/exit/relaunch cycle confirms no stale input/overlay references are retained.

## Links

- Related docs: `docs/process-improvement-log.md`
- Related PR: `TBD`
- Related commit(s): `TBD`

