# COH-033 - Replace `GameGrubApp` companion runtime globals with lifecycle-owned state holders

- **ID**: `COH-033`
- **Area**: `app runtime state + ui/xserver interop`
- **Priority**: `P1`
- **Status**: `Done`
- **Owner**: `Sisyphus`
- **Documentation Impact**: `No doc changes required beyond this ticket update; scope was code-level ownership migration.`
- **Reviewer**: `TBD`

## Implementation Progress (2026-04-09)

### Completed
- [x] Created `ui/runtime/XServerRuntimeState.kt` class
- [x] Added deprecated forwarders in `GameGrubApp` companion during migration phase
- [x] Migrated `GameGrubApp` runtime global accesses to lifecycle-owned holders
- [x] Removed `GameGrubApp` companion runtime globals and static call sites (`GameGrubApp.` no longer used in app Kotlin sources)

### Remaining Work
- [x] None for this ticket scope

## Problem

`GameGrubApp` companion object retains mutable global references to runtime objects and UI views (`xServerView`, `inputControlsView`, `touchpadView`, listener refs), creating memory leak and thread-safety risk.

## Scope

- In scope:
  - Inventory and classify companion globals by ownership and lifecycle.
  - Replace long-lived view/listener references with lifecycle-owned holders and explicit cleanup points.
  - Introduce thread-safe access patterns for remaining shared runtime session state.
- Out of scope:
  - Redesigning Winlator runtime behavior.

## Dependencies and Decomposition

- Parent ticket: `todo/COH-032.md`
- Child tickets: `N/A`
- Related follow-ups: `todo/DOC-015.md`, `todo/TEST-004.md`
- Blocker (if `Blocked`): `N/A`

## Acceptance Criteria

- [x] `GameGrubApp` companion no longer stores direct `View` or listener references that can outlive their owners.
- [x] Remaining shared state has explicit owner, lifecycle reset path, and thread-safety strategy.
- [x] Leak-prone TODO comments in this area are resolved or replaced by tracked owner/action notes.
- [x] Documentation updated (or `No doc changes required` note added with reason).

## Validation

- [ ] `./gradlew testDebugUnitTest --tests "*Launch*"`
- [ ] `./gradlew lintKotlin`
- [ ] Manual start/exit/relaunch cycle confirms no stale input/overlay references are retained.

## Links

- Related docs: `docs/process-improvement-log.md`
- Related PR: `TBD`
- Related commit(s): `TBD` (companion-object removal in `app/src/main/java/app/gamegrub/GameGrubApp.kt`)

