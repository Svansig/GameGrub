# UI-005a - Extract preLaunchApp into LaunchCoordinator

- **ID**: `UI-005a`
- **Area**: `ui/GameGrubMain.kt`
- **Priority**: `P1`
- **Status**: `Backlog`
- **Owner**: `Sisyphus`
- **Parent**: `todo/UI-005.md`

## Problem

`preLaunchApp` function (lines 1465-2026) is a 560+ line monolith in GameGrubMain that handles game launch orchestration. This makes it hard to test and reason about.

## Scope

- In scope:
  - Extract `preLaunchApp` function into a `LaunchCoordinator` class/object
  - Use Hilt injection for the coordinator
  - Ensure all existing parameters and callbacks work the same
  - Move unmanaged `CoroutineScope(Dispatchers.IO).launch` to lifecycle-managed scope
- Out of scope:
  - Changing any launch logic behavior

## Acceptance Criteria

- [ ] `LaunchCoordinator` class created with `preLaunch` method
- [ ] All existing parameters preserved (same signature)
- [ ] `CoroutineScope(Dispatchers.IO)` replaced with proper coroutine scope
- [ ] GameGrubMain calls coordinator instead of standalone function
- [ ] No behavior changes in launch flow

## Validation

- [ ] Build compiles
- [ ] Lint passes
- [ ] Manual test of launch flow