# UI-005f - Extract service startup lifecycle policy from GameGrubMain

- **ID**: `UI-005f`
- **Area**: `ui/GameGrubMain.kt + session/service startup`
- **Priority**: `P1`
- **Status**: `Done`
- **Owner**: `Sisyphus`
- **Parent**: `todo/UI-005.md`
- **Documentation Impact**: `Ticket docs updated to track service-start ownership migration.`

## Problem

`GameGrubMain` currently decides when to start Steam/GOG/Epic/Amazon services inside `repeatOnLifecycle`, which couples composable lifecycle to service policy logic.

## Scope

- In scope:
  - Move service startup/reconnect policy evaluation out of `GameGrubMain` into a coordinator/use-case boundary.
  - Keep existing startup conditions and route behavior unchanged.
  - Keep `GameGrubMain` as a caller that forwards lifecycle events/intents.
- Out of scope:
  - Service implementation redesign.
  - Login/auth protocol changes.

## Acceptance Criteria

- [ ] Service startup decision logic no longer resides directly in `GameGrubMain`.
- [ ] `GameGrubMain` delegates startup orchestration to a dedicated boundary.
- [ ] Existing reconnect/start behavior parity is preserved for Steam/GOG/Epic/Amazon.

## Validation

- [ ] `./gradlew testDebugUnitTest --tests "*MainViewModel*"`
- [ ] `./gradlew lintKotlin`
- [ ] Manual check: app cold start and resume start the same services under same conditions

