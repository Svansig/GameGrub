# UI-005h - Move container config save/discard side effects out of GameGrubMain dialog handlers

- **ID**: `UI-005h`
- **Area**: `ui/GameGrubMain.kt + container config flow`
- **Priority**: `P1`
- **Status**: `Done`
- **Owner**: `Sisyphus`
- **Parent**: `todo/UI-005.md`
- **Documentation Impact**: `Ticket docs updated for container-config ownership boundary.`

## Problem

`GameGrubMain` directly performs container config persistence/restore operations (`IntentLaunchManager` + `ContainerUtils`) inside dialog action handlers.

## Scope

- In scope:
  - Extract save/discard/restore side effects into a dedicated coordinator/use-case.
  - Keep dialog behavior and temporary override semantics unchanged.
- Out of scope:
  - Container model schema changes.
  - Container editor UI redesign.

## Acceptance Criteria

- [ ] Container config write/restore side effects are no longer executed directly in composable dialog callbacks.
- [ ] `GameGrubMain` delegates config action handling to one boundary API.
- [ ] Save/discard/dismiss behavior parity remains intact.

## Validation

- [ ] `./gradlew testDebugUnitTest --tests "*Container*"`
- [ ] `./gradlew lintKotlin`
- [ ] Manual check: save config, discard config, close dialog restoration behavior

