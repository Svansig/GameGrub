# UI-007 - Align architecture doc UI path naming

- **ID**: `UI-007`
- **Area**: `docs`
- **Priority**: `P2`
- **Status**: `Done`
- **Owner**: `Sisyphus`

## Problem

Documentation has historically referenced outdated UI folder names, causing placement confusion.

## Scope

- In scope:
  - Ensure architecture/developer docs consistently use `ui/component` and `ui/utils`.
  - Cross-link to local ticket system.
- Out of scope:
  - Code refactors.

## Acceptance Criteria

- [x] Core docs use canonical UI paths.
- [x] Ticket workflow references are present in contributor entry docs.

## Validation

- [x] Doc links resolve - ARCHITECTURE.md, AGENTS.md both use correct `ui/component/` and `ui/utils/` paths.
- [x] Paths in docs match repository structure - Verified: `ui/component/`, `ui/utils/` exist as `ui/component` and `ui/utils` in repo.
- [x] Ticket workflow references present in README.md (lines 36-46) and AGENTS.md (lines 262, 345, 352).

## Documentation Impact

No doc changes required - audit confirmed existing docs are already aligned:
- ARCHITECTURE.md: Uses `ui/component/` and `ui/utils/` (correct)
- AGENTS.md: Uses `ui/component/` with note about `ui/components/` being legacy/outdated (correct)
- README.md: Has ticket system references in Local Tickets section

## Links

- Related docs: `ARCHITECTURE.md`, `AGENTS.md`, `docs/ui-placement/README.md`
- PR: `TBD`

