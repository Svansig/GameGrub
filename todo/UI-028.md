# UI-028 - Localize hardcoded user-facing strings in main/settings/feedback flows

- **ID**: `UI-028`
- **Area**: `ui/GameGrubMain.kt + ui/screen/settings + ui/feedback`
- **Priority**: `P1`
- **Status**: `In Progress`
- **Owner**: `Copilot`
- **Documentation Impact**: `No doc changes required` - Existing wording migrated to string resources for localization consistency.
- **Reviewer**: `TBD`

## Problem

Hardcoded user-facing strings remain in key UI paths (main dialogs/snackbars, settings debug UX, feedback submission messaging), creating localization drift and inconsistent copy ownership.

## Scope

- In scope:
  - Move audited user-facing literals to `app/src/main/res/values/strings.xml`.
  - Replace call sites with `stringResource(...)` / `context.getString(...)`.
  - Preserve existing behavior, severity, and dynamic placeholders.
- Out of scope:
  - Copywriting redesign or product wording changes.

## Dependencies and Decomposition

- Parent ticket: `N/A`
- Child tickets: `N/A`
- Related follow-ups: `todo/READ-011.md`, `todo/UI-005k.md`
- Blocker (if `Blocked`): `N/A`

## Acceptance Criteria

- [ ] Hardcoded user-visible strings identified in `docs/magic-literals-audit-2026-04-04.md` are resource-backed.
- [ ] Compose call sites use `stringResource(...)`; non-Compose call sites use `context.getString(...)`.
- [ ] Dynamic string interpolation uses Android resource placeholders.
- [ ] No behavior changes beyond string sourcing.

## Validation

- [ ] `./gradlew testDebugUnitTest --tests "*MainViewModel*"` *(blocked locally: `JAVA_HOME` not set in current shell)*
- [ ] `./gradlew lintKotlin` *(blocked locally: `JAVA_HOME` not set in current shell)*
- [ ] Manual checks: affected dialogs/snackbars still render expected text.

### Validation Notes

- Attempted: `./gradlew :app:compileDebugKotlin --console=plain`
- Result: build tooling unavailable in this environment (`JAVA_HOME is not set and no 'java' command could be found in your PATH`).

## Links

- Related docs: `docs/magic-literals-audit-2026-04-04.md`
- Related PR: `TBD`
- Related commit(s): `TBD`

